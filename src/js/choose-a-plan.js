window.addEventListener('DOMContentLoaded', () => {
    var app = new Vue({
        el: '#choose-a-plan-container',
        data () {
            return {
                step: 1
            }
        },
        methods: {
            submitForm () {
                // some function here
            }
        }
    })
})
